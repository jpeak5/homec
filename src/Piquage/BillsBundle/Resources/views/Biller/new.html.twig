{% extends '::base.html.twig' %}
{% block body %}

{% if button == 'Add' %}

<form action="{{ path('new_biller') }}" method="post" {{ form_enctype(form) }}>

{% endif %}
{% if button == 'Update'%}

    <form action="{{ path('edit_biller', {'name': form.vars.value.name}) }}" method="post" {{ form_enctype(form) }}>

{% endif %}
    {{ form_widget(form) }}

    <input type="submit" value="{{button}}"/>
</form>

{% endblock %}